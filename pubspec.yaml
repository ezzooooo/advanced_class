name: advanced_class
description: "Flutter Advanced Class - 실무 역량 강화 학습 프로젝트"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # 5주차: FCM Push Notification
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.3
  flutter_local_notifications: ^19.2.1
  
  # 6주차: 앱 모니터링
  firebase_analytics: ^11.4.3
  firebase_crashlytics: ^4.3.3
  
  # 7주차: 보안
  flutter_secure_storage: ^9.2.4
  package_info_plus: ^9.0.0
  firebase_remote_config: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # 1주차: 테스팅
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
